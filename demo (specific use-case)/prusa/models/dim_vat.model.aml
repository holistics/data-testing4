Model dim_vat {
  type: 'query'
  label: 'VAT'
  data_source_name: 'data_staging'
  query: @sql
SELECT * FROM UNNEST([
  STRUCT('VAT excluded' AS vat),
  STRUCT('VAT included' AS vat)
]);;
  dimension vat {
    label: 'VAT'
    type: 'text'
    primary_key: true
  }
}
